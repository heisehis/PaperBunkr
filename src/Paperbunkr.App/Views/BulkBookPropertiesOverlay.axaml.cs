using Avalonia.Controls;

namespace Paperbunkr.App.Views;

/// <summary>
/// Code-behind for <see cref="BulkBookPropertiesOverlay"/> - intentionally minimal. Present from the
/// first commit alongside the .axaml (CLAUDE.md's AVLN2000 build gotcha).
/// </summary>
public partial class BulkBookPropertiesOverlay : UserControl
{
    public BulkBookPropertiesOverlay()
    {
        InitializeComponent();
    }
}
