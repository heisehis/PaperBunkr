// NetSearch hook - proves the Query payload arrives and a NetSearchResult list round-trips.
return new[] { new NetSearchResult("plugin-1", "Plugin Result for " + Query, "https://example.com/plugin-1", 0.99) };
